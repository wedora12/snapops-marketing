import { SCENE_ID } from "@/scenes/Scene05_Trust/constants";
import type { SceneProps } from "@/scenes/types";

export default function Scene05Trust({ className }: SceneProps) {
  return <section data-scene={SCENE_ID} className={className} />;
}
