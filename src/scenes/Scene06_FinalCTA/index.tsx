import { SCENE_ID } from "@/scenes/Scene06_FinalCTA/constants";
import type { SceneProps } from "@/scenes/types";

export default function Scene06FinalCTA({ className }: SceneProps) {
  return <section data-scene={SCENE_ID} className={className} />;
}
