export type MagneticAnimationConfig = {
  strength?: number;
  radius?: number;
};

export function createMagneticAnimation(_config: MagneticAnimationConfig = {}): void {
  // implementation pending
}
