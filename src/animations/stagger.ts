export type StaggerAnimationConfig = {
  stagger?: number;
  duration?: number;
  ease?: string;
};

export function createStaggerAnimation(_config: StaggerAnimationConfig = {}): void {
  // implementation pending
}
