export type ScrollAnimationConfig = {
  start?: string;
  end?: string;
  scrub?: boolean | number;
  pin?: boolean;
};

export function createScrollAnimation(_config: ScrollAnimationConfig = {}): void {
  // implementation pending
}
